---
const { PageColour, platform, username, tld } = Astro.props;
---
<a class=`py-2 px-4 ${PageColour} bg-mantle no-underline rounded-full` href={`https://www.${platform}.${tld}/${username}`}>{platform}</a>