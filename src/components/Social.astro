---
const { PageColour, platform, username, tld } = Astro.props;
---
<a class=`py-2 px-4 ${PageColour} bg-mantle no-underline rounded-full hover:text-mantle hover:bg-${PageColour.slice(5)}` href={`https://${platform}.${tld}/${username}`}>{platform}</a>